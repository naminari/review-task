# PR Reviewer Assignment Service

Микросервис для автоматического назначения ревьюеров на Pull Request'ы внутри команды.  
Разработан в рамках тестового задания для стажёра Backend (осенняя волна 2025).

---

## 🚀 Быстрый старт

### Требования
- Docker  
- Docker Compose

### Запуск

```bash
git clone <https://github.com/naminari/review-task>
cd review-task

make docker-up

Сервис будет доступен по адресу: http://localhost:8080

---

## 🔍 Проверка работоспособности

make health

Ожидаемый ответ:

{"status":"OK","db":"connected"}

---

## 🧪 Тестирование

### Полный запуск всех тестов

make test

### Другие команды

| Команда | Описание |
|--------|----------|
| make unit-test | Запуск unit-тестов |
| make quick-test | Быстрая проверка API (генерирует тестовые данные) |
| make examples | Демонстрационные примеры (работают после make quick-test) |


> ⚠️ Важно:  
> make examples выполняется только после `make quick-test`, так как примеры используют тестовые данные.

---

## 📚 API Endpoints

Сервис реализует REST API и полностью соответствует спецификации OpenAPI 3.0 (см. openapi.yaml).

### 🔧 Управление командами
| Method | Endpoint | Описание |
|--------|----------|----------|
| POST | /team/add | Создать команду |
| GET | /team/get | Получить информацию о команде |

### 👤 Пользователи
| Method | Endpoint | Описание |
|--------|----------|----------|
| POST | /users/setIsActive | Изменить флаг активности |
| GET | /users/getReview | Получить PR, где пользователь — ревьюер |

### 🔄 Pull Request'ы
| Method | Endpoint | Описание |
|--------|----------|----------|
| POST | /pullRequest/create | Создать PR и автоматически назначить ревьюеров |
| POST | /pullRequest/merge | Мердж PR |
| POST | /pullRequest/reassign | Переназначить ревьюера |

### 🏥 Healthcheck
| Method | Endpoint | Описание |
|--------|----------|----------|
| GET | /health | Проверка статуса сервиса |

---

## 🏗 Архитектура

### Технологии

| Компонент | Стек |
|-----------|------|
| Язык | Go 1.21+ |
| Web Framework | Gin |
| БД | PostgreSQL |
| Контейнеризация | Docker + Docker Compose |

### 📁 Структура проекта

review-task/
├── main.go
├── openapi.yaml
├── Makefile
├── docker-compose.yml
├── migrations/
├── handlers/
├── service/
├── repository/
├── models/
├── database/
└── scripts/

---

## 🔧 Разработка

make run           # локальный запуск без Docker
make build         # сборка бинарника
make docker-logs   # просмотр логов
make docker-down   # остановка

Полная очистка БД:

make docker-down
docker volume rm review-task_postgres_data

---

## 📊 Бизнес-логика

### Автоматическое назначение ревьюеров

- Назначается до 2 активных участников команды автора
- Автор PR не может быть ревьюером
- Неактивные пользователи исключаются
- Если доступен только один — назначается один

### Переназначение ревьюеров

- Замена возможна только если PR не смержен
- Новый ревьюер выбирается из активных участников команды

### Идемпотентность

- Повторный вызов /merge не изменяет состояние

---

## 🛑 Ошибки

| Код | Значение |
|-----|----------|
| TEAM_EXISTS | Команда уже существует |
| PR_EXISTS | PR уже существует |
| PR_MERGED | PR уже смержен |
| NOT_ASSIGNED | Пользователь не ревьюер |
| NOT_FOUND | Объект не найден |

---

## 🐳 Docker

Файл .env:

DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=review_service
APP_PORT=8080
GIN_MODE=release

Миграции применяются автоматически при запуске.

---

## ✔️ Соответствие требованиям

| Требование | Выполнено |
|------------|----------|
| 7 эндпоинтов | ✅ |
| Соответствие OpenAPI | ✅ |
| Автоматическое назначение ревьюеров | ✅ |
| Docker Compose + миграции | ✅ |
| Набор тестов | ✅ |

---

## 📈 Производительность

- Оптимизировано под ~20 команд, ~200 пользователей
- Среднее время ответа: < 300ms
- Используются индексы PostgreSQL

---

### 🏁 Статус

Проект завершён в рамках тестового задания для стажёра Backend (осень 2025).

